{
  "name": "jcore-cli",
  "version": "1.0.0",
  "scripts": {
    "build": "esbuild main.ts --bundle --platform=node --outfile=bin/jcore",
    "watch": "esbuild main.ts --bundle --watch --platform=node --outfile=bin/jcore"
  },
  "devDependencies": {
    "@types/node": "^18.11.18",
    "esbuild": "^0.16.17",
    "typescript": "^4.9.4"
  },
  "dependencies": {
  }
}
